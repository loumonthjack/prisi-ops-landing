/**
 * Chart Theme Configuration
 *
 * Consistent styling for Recharts components matching
 * the minimalist luxury design system.
 */
export const chartTheme = {
  colors: {
    primary: 'var(--text)',
    secondary: 'var(--text-secondary)',
    muted: 'var(--text-muted)',
    border: 'var(--border)',
    positive: 'var(--trend-positive)',
    negative: 'var(--trend-negative)',
  },
  fonts: {
    body: 'var(--font-body)',
    mono: 'var(--font-mono)',
  },
  sizes: {
    fontSize: 12,
    labelFontSize: 11,
  }
};
